---
title: "Kafka: события, команды, Topics и Keys — Часть 6"
date: 2026-08-06T14:00:00+03:00
description: "Разбираем Kafka как очередь задач и потоковую платформу, а затем переходим к production-проектированию Topics, Keys, Partitions, Retention и формата событий."
tags: [kafka]
thumbnail: "images/image.png"
toc: true
draft: true
series: "kafka-from-zero-to-hero"
tldr: "Kafka может работать и как очередь задач, и как поток событий. Production-дизайн начинается с выбора типа сообщения, границ Topic, Key, количества Partition, Retention и контракта события."
---

До этого момента мы разбирали внутреннее устройство Kafka: `Partition`, `Offset`, репликацию, `Leader`, `Follower`, `ISR`, Page Cache и batching. Теперь пора перейти от механики к архитектуре.

Главный вопрос уже не «как Kafka хранит сообщения?», а «какие сообщения вообще стоит через неё передавать?». От ответа зависят `Topic`, `Key`, количество `Partition`, `Retention`, схема события и даже способ обработки ошибок.

## Kafka в роли очереди задач

В классической модели очереди одна задача должна быть обработана одним воркером. В Kafka это реализуется через `Consumer Group`: каждую `Partition` внутри группы в конкретный момент читает только один `Consumer`.

```text
               video-processing
             /        |        \
       Partition 0 Partition 1 Partition 2
            |         |         |
        Worker A  Worker B  Worker C
        \_______________________/
          Consumer Group: transcoding-workers
```

Такая модель подходит для фоновых задач:

- транскодирование видео;
- генерация отчётов;
- отправка email;
- импорт данных;
- тяжёлая асинхронная обработка.

Kafka в этом случае работает как буфер нагрузки. Если producer отправляет 100 000 сообщений в секунду, а воркеры успевают обработать только 20 000, данные остаются в логе и ждут обработки. В отличие от классической очереди, сообщение не удаляется сразу после чтения: `Consumer Group` просто продвигает свой `Offset`.

## Kafka как поток событий

В потоковой модели сообщение — это не команда «сделай действие», а факт: что-то уже произошло.

```text
OrderPaid
UserRegistered
FlightPriceChanged
```

Один и тот же поток могут независимо читать разные сервисы:

```text
OrderPaid
   |
   +--> delivery-group
   |
   +--> analytics-group
   |
   +--> fraud-detection-group
```

Каждая `Consumer Group` хранит собственный `Offset`, поэтому обработчики не мешают друг другу и читают поток со своей скоростью. Именно это делает Kafka не просто message broker, а Event Streaming Platform.

## Queue vs Stream

| Характеристика | Модель очереди | Потоковая модель |
| :--- | :--- | :--- |
| Главный вопрос | Кто должен сделать задачу? | Кому интересен факт? |
| Тип сообщения | Команда или задача | Событие |
| Обработка | Один worker внутри группы | Несколько независимых групп |
| Жизненный цикл | Задача считается выполненной | Событие хранится до retention |
| Типичные кейсы | Email, отчёты, видео | Аналитика, антифрод, CDC |

На практике один `Topic` может обслуживать обе модели. Например, `orders.events` читает группа доставки как рабочую очередь, а аналитика и антифрод читают тот же поток как последовательность бизнес-фактов.

## Почему это важно для проектирования

Понимать устройство Kafka недостаточно. Большинство production-проблем появляется не потому, что Kafka работает неправильно, а потому, что до запуска были неверно выбраны:

- структура `Topics`;
- количество `Partition`;
- ключ сообщения;
- срок хранения данных;
- модель совместимости событий.

На тестовой нагрузке такие ошибки почти незаметны. Через несколько месяцев одна `Partition` оказывается перегруженной, `Consumer` начинают отставать, а изменение формата события ломает сразу несколько сервисов. Поэтому проектирование Kafka начинается не с команды создания топика, а с ответов на архитектурные вопросы.

## Сначала определите назначение события

До создания `Topic` необходимо ответить на главный вопрос:

> Что именно передаётся через Kafka — команда, факт или техническое сообщение?

Рассмотрим все три варианта.

### Команда

Команда просит конкретный сервис выполнить действие.

```text
GenerateInvoice

SendEmail

ReserveInventory
```

У команды обычно есть ожидаемый исполнитель.

Например:

```text
Order Service
      |
      v
GenerateInvoice
      |
      v
Billing Service
```

Такие сообщения ближе к модели очереди задач.

### Событие

Событие фиксирует факт, который уже произошёл.

```text
OrderCreated

PaymentCompleted

UserRegistered
```

У события нет единственного получателя.

На него могут подписаться:

- аналитика;
- уведомления;
- антифрод;
- биллинг;
- CRM;
- хранилище данных.

```text
                  OrderCreated
                       |
        +--------------+--------------+
        |              |              |
        v              v              v
    Analytics      Notification      Fraud
```

Именно события лучше всего соответствуют Event-Driven архитектуре.

### Техническое сообщение

Иногда Kafka используется для внутренних инфраструктурных потоков.

Например:

```text
application-logs

database-changes

metrics-events

audit-records
```

У таких `Topics` могут быть другие требования:

- большой объём;
- короткий `Retention`;
- высокая степень сжатия;
- множество `Partition`;
- отсутствие строгого порядка между объектами.

## Не создавайте один Topic для всего

Одна из первых ошибок — отправлять все события системы в общий `Topic`. Это удобно примерно до того момента, пока общий Topic не превращается в свалку с красивым названием `events`.

```text
events

UserRegistered

OrderCreated

PaymentFailed

EmailSent

ProductUpdated
```

На первый взгляд это удобно.

`Producer` используют одну точку записи, `Consumer` фильтруют сообщения по полю `type`.

Но со временем появляются проблемы:

- разные события требуют разного `Retention`;
- нагрузка распределяется неравномерно;
- права доступа становятся слишком широкими;
- изменение одного формата влияет на множество `Consumer`;
- невозможно независимо масштабировать отдельные потоки;
- сложнее отслеживать отставание `Consumer`.

Лучше разделять `Topics` по логическому назначению.

```text
users.events

orders.events

payments.events

notifications.commands
```

## Но Topic на каждый тип события — тоже не всегда хорошо

Противоположная крайность:

```text
order-created

order-paid

order-cancelled

order-shipped

order-delivered
```

При большом количестве сущностей Kafka-кластер быстро обрастает сотнями или тысячами `Topics`.

Это усложняет:

- администрирование;
- мониторинг;
- настройку прав;
- управление конфигурацией;
- поддержку схем.

Нередко разумнее объединить события одной бизнес-сущности.

```text
orders.events
```

А тип события хранить внутри сообщения:

```json
{
  "eventType": "OrderPaid",
  "eventId": "5c72a8f1",
  "orderId": "48291",
  "occurredAt": "2026-08-06T09:30:00Z",
  "data": {
    "amount": 15990,
    "currency": "RUB"
  }
}
```

## Как именовать Topics

Имя должно помогать понять назначение потока без чтения документации.

Можно использовать структуру:

```text
<domain>.<entity>.<message-type>
```

Хороший пример именования топиков будет:

```text
commerce.orders.events

commerce.payments.events

identity.users.events

notifications.email.commands
```

Главное правило:

> Соглашение должно быть единым для всей компании.

Плохой вариант:

```text
orders

payment_events_v2

UserUpdateTopic

email-queue-prod-new
```

## Как выбрать Key

`Key` определяет, в какую `Partition` попадёт сообщение.

Следовательно, он влияет сразу на три свойства:

- порядок;
- распределение нагрузки;
- возможность масштабирования.

Выбор ключа — одно из важнейших архитектурных решений.

### Пример с заказами

Для событий заказа естественным ключом будет `orderId`.

```json
{
  "key": "order-48291",
  "eventType": "OrderPaid"
}
```

Все события одного заказа попадут в одну `Partition`:

```text
OrderCreated

PaymentStarted

PaymentCompleted

DeliveryStarted

OrderDelivered
```

Порядок будет сохранён.

### Пример с пользователями

Для потока действий пользователя ключом может быть `userId`.

```text
user-1732
```

Тогда Kafka сохранит порядок событий одного пользователя:

```text
UserRegistered

EmailChanged

PasswordChanged

UserBlocked
```

При этом события разных пользователей смогут обрабатываться параллельно.

### Плохой ключ: константа

Представим, что у всех сообщений одинаковый `Key`:

```text
key = orders
```

Все данные попадут в одну `Partition`.

```text
Partition 0: 100% нагрузки

Partition 1: 0%

Partition 2: 0%
```

Kafka-кластер может состоять из десятков `Broker`, но вся нагрузка всё равно будет проходить через один из них. Так возникает **hot partition**.

### Плохой ключ: поле с малым количеством значений

Допустим, ключом выбран статус заказа:

```text
created

paid

cancelled
```

Значений всего три. При десяти `Partition` большая часть из них может вообще не использоваться. Кроме того, значение `created` будет встречаться гораздо чаще остальных и создаст перекос нагрузки.

### Плохой ключ: текущее время

Иногда для равномерного распределения используют `Timestamp`.

```text
key = 2026-08-06T09:30:01
```

Нагрузка действительно может распределиться. Но события одного заказа начнут попадать в разные `Partition`.

```text
OrderCreated    -> Partition 1

PaymentStarted  -> Partition 4

PaymentSuccess  -> Partition 0
```

Порядок между ними больше не гарантируется.

## Как избежать Hot partition

Hot partition появляется, когда один ключ встречается значительно чаще остальных.

Например, `Topic` хранит события продавцов маркетплейса.

Ключ:

```text
sellerId
```

Один крупный продавец создаёт половину всех событий.

В результате:

```text
seller-1 -> Partition 2 -> 50% трафика

остальные продавцы -> другие Partition
```

### Возможные решения: Использовать составной ключ

```text
sellerId + shard
```

Например:

```text
seller-1-0

seller-1-1

seller-1-2
```

Это распределит события крупного продавца между несколькими `Partition`.

Но глобальный порядок всех событий продавца будет потерян.

Порядок сохранится только внутри каждого shard.

### Возможные решения: Выбрать более гранулярный объект

Вместо:

```text
sellerId
```

можно использовать:

```text
orderId
```

Если порядок нужен только внутри заказа, это будет более правильный ключ.

### Возможные решения: Разделить поток

Особенно крупные источники иногда выносят в отдельный `Topic`.

```text
orders.events

orders.large-sellers.events
```

Но такое решение увеличивает сложность и требует веских причин. Тем не менее — в некоторых кейсах это может быть вполне оправдано.

## Сколько Partition создавать

Универсального числа не существует.

Количество зависит от:

- объёма данных;
- требуемой пропускной способности;
- количества `Consumer`;
- требований к порядку;
- размера сообщений;
- производительности `Broker`;
- перспектив роста нагрузки.

Важный принцип:

> Максимальный параллелизм одной Consumer Group ограничен количеством Partition.

Если в `Topic` четыре `Partition`:

```text
P0

P1

P2

P3
```

эффективно работать одновременно смогут максимум четыре `Consumer`.

```text
P0 -> Consumer A

P1 -> Consumer B

P2 -> Consumer C

P3 -> Consumer D
```

Пятый `Consumer` будет простаивать.

### Почему нельзя просто создать тысячу Partition

Каждая `Partition` требует ресурсов:

- файловых дескрипторов;
- памяти;
- метаданных;
- сетевых соединений;
- операций репликации;
- времени на восстановление;
- времени на `Consumer Rebalance`.

Чем больше `Partition`, тем сложнее кластеру:

- выбирать лидеров;
- перераспределять данные;
- восстанавливаться после падения `Broker`;
- выполнять `Rebalance Consumer Group`.

Поэтому принцип «чем больше, тем лучше» здесь не работает.

### Простой подход к расчёту

Допустим, один `Consumer` обрабатывает:

```text
5 000 сообщений в секунду
```

Ожидаемая нагрузка:

```text
40 000 сообщений в секунду
```

Минимально потребуется:

```text
40 000 / 5 000 = 8 Consumer
```

Следовательно, нужно как минимум восемь `Partition`.

Но обычно оставляют запас на рост:

```text
12 или 16 Partition
```

Это не строгая формула, а отправная точка для нагрузочного тестирования.

### Планируйте рост заранее

Увеличить количество `Partition` можно. Уменьшить — значительно сложнее. Однако после увеличения меняется результат маршрутизации по ключу:

```text
hash(key) % partition_count
```

Было:

```text
hash(key) % 4
```

Стало:

```text
hash(key) % 8
```

Некоторые новые сообщения начнут попадать в другие `Partition`. Из-за этого порядок старых и новых событий одного ключа может нарушиться. Поэтому количество `Partition` следует выбирать с учётом будущего роста.

## Retention: сколько хранить сообщения

Kafka не удаляет сообщение после того, как его прочитал `Consumer`. Удаление определяется политикой хранения, которые мы разберем далее. Но нельзя выбирать `Retention` случайно.

Сначала нужно ответить на вопросы:

- сколько времени `Consumer` может быть недоступен;
- нужно ли переигрывать историю;
- есть ли данные в другом источнике;
- существуют ли юридические ограничения;
- сколько дискового пространства доступно;
- насколько быстро растёт `Topic`.

Например, если `Consumer` может не работать три дня, а `Retention` равен одному дню, после восстановления он уже не сможет получить пропущенные события.

### Хранение по времени

Например:

```text
Retention = 7 дней
```

Kafka удаляет сегменты журнала старше этого срока.

Такой режим подходит для:

- технических событий;
- логов;
- промежуточных потоков;
- данных, которые можно восстановить из основной системы.

### Хранение по размеру

Можно ограничить максимальный объём `Partition`. Когда лимит превышен — старые сегменты удаляются. Важно понимать, что ограничение обычно применяется к каждой `Partition` отдельно. Если `Topic` состоит из десяти `Partition`, общий объём может быть значительно больше одного установленного лимита.

### Log Compaction

Иногда необходимо хранить не всю историю, а последнее состояние каждого ключа.

Представим поток:

```text
user-1 -> name=Ivan

user-2 -> name=Anna

user-1 -> name=Sergey
```

После compaction Kafka может сохранить:

```text
user-1 -> name=Sergey

user-2 -> name=Anna
```

Предыдущая версия `user-1` со временем удаляется.

Такой подход полезен для:

- CDC;
- потоков текущего состояния;
- восстановления локального кэша;
- таблиц конфигурации;
- профилей пользователей.

## Проектируйте формат события

Kafka передаёт байты, но сервисам нужен контракт.

Плохой вариант:

```json
{
  "id": 123,
  "value": "something"
}
```

Непонятно:

- что такое `id`;
- что означает `value`;
- какая версия сообщения используется;
- когда событие произошло;
- как обнаруживать дубликаты.

Лучше использовать предсказуемую структуру.

```json
{
  "eventId": "0ae2342d-105d-45ad-8d91-441b7115afbc",
  "eventType": "OrderCreated",
  "eventVersion": 1,
  "occurredAt": "2026-08-06T09:30:00Z",
  "producer": "order-service",
  "data": {
    "orderId": "48291",
    "userId": "1732",
    "total": 15990,
    "currency": "RUB"
  }
}
```

### Зачем нужен Event ID

При модели at-least-once `Consumer` может получить одно сообщение повторно. Уникальный идентификатор позволяет проверить, было ли событие уже обработано.

```text
eventId = 0ae2342d
```

`Consumer` сохраняет обработанные идентификаторы или использует их как уникальный ключ бизнес-операции.

### Зачем нужна версия события

Формат сообщений со временем меняется.

Первая версия:

```json
{
  "orderId": "48291",
  "total": 15990
}
```

Вторая версия:

```json
{
  "orderId": "48291",
  "amount": {
    "value": 15990,
    "currency": "RUB"
  }
}
```

Старые `Consumer` могут не понимать новый формат. Поле версии помогает явно управлять совместимостью:

```text
eventVersion = 2
```

### Не меняйте смысл существующего поля

Допустим, поле `price` сначала хранит рубли:

```json
{
  "price": 1500
}
```

После обновления оно начинает хранить копейки:

```json
{
  "price": 150000
}
```

Формат остался числом, но смысл изменился. Это особенно опасно: `Consumer` не упадут с ошибкой, а начнут незаметно обрабатывать неправильные данные. Лучше добавить новое поле:

```json
{
  "amountMinor": 150000,
  "currency": "RUB"
}
```

### Предпочитайте совместимые изменения

Обычно безопаснее:

- добавлять необязательные поля;
- задавать значения по умолчанию;
- не удалять используемые поля;
- не менять тип поля;
- не переиспользовать имя поля с другим смыслом.

Опасные изменения:

```text
string -> integer

optional -> required

поле удалено

значение изменило смысл
```

### Когда нужен Schema Registry

Поле `eventVersion` помогает читателю и коду понять версию события, но в production этого обычно недостаточно. Если один поток читают несколько команд, нужен формальный контракт: Avro, Protobuf или JSON Schema и реестр схем.

Schema Registry решает три практические задачи:

- хранит версии схем для каждого потока;
- проверяет совместимость изменений до публикации;
- не даёт случайно выпустить событие, которое сломает старые `Consumer`.

На практике стоит заранее выбрать режим совместимости. Например, `BACKWARD` позволяет новым `Consumer` читать старые сообщения, а `FORWARD` помогает старым `Consumer` пережить часть новых сообщений. Конкретный режим зависит от того, как долго живут старые события и как быстро обновляются потребители.

Хорошее правило: если событие является контрактом между командами, его схема должна проходить автоматическую проверку так же, как код проходит тесты.


## Документируйте владельца Topic

Для каждого `Topic` полезно знать:

- какая команда им владеет;
- какие сервисы публикуют данные;
- какие события находятся внутри;
- какой `Key` используется;
- сколько длится `Retention`;
- какая схема сообщения;
- какие гарантии порядка существуют;
- какие `Consumer` считаются критичными.

Пример карточки `Topic`:

```text
Topic:
commerce.orders.events

Owner:
Order Platform Team

Key:
orderId

Partitions:
12

Replication Factor:
3

Retention:
14 days

Schema:
OrderEvent v3

Ordering:
Guaranteed per orderId
```

Без такой документации Kafka быстро превращается в непрозрачную систему, где никто не знает, можно ли удалить старый `Topic` или изменить формат события.

## Контрольный список перед запуском

Перед созданием `Topic` полезно ответить на следующие вопросы.

### Назначение

```text
Это команда или событие?

Кто Producer?

Кто Consumer?

Можно ли добавить новых Consumer позже?
```

### Порядок

```text
Для каких объектов важен порядок?

Какой Key его обеспечивает?

Нет ли риска горячего ключа?
```

### Масштабирование

```text
Какова ожидаемая нагрузка?

Сколько Consumer потребуется?

Хватит ли количества Partition через год?
```

### Хранение

```text
Сколько времени хранить сообщения?

Нужно ли повторное проигрывание?

Что произойдёт, если Consumer не работает несколько дней?
```

### Контракт

```text
Есть ли Event ID?

Есть ли версия схемы?

Совместимы ли изменения со старыми Consumer?
```

### Эксплуатация

```text
Кто владеет Topic?

Какие метрики и алерты настроены?

Как отслеживается Consumer Lag?
```


## Итог

Kafka начинается не с установки кластера и не с создания `Topic`. Она начинается с ответов на вопросы:

- что является событием, командой или техническим сообщением;
- для какого объекта нужен порядок;
- какой `Key` обеспечит этот порядок;
- как поток будет масштабироваться;
- сколько времени данные должны храниться;
- как развивать формат данных;
- кто владеет потоком и его контрактом.

Правильно выбранные `Topic`, `Key`, `Partition`, `Retention` и схема события позволяют Kafka масштабироваться годами. Ошибки в этих решениях могут сделать даже мощный кластер медленным, нестабильным и сложным в сопровождении.

## Что дальше?

В следующей части разберём надёжность и эксплуатацию Kafka-пайплайна: `acks`, `min.insync.replicas`, идемпотентного producer, commit offset, retry, DLQ, Transactional Outbox, Consumer Lag и типовые production-настройки.
